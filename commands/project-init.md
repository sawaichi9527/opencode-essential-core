---
description: Scaffold a brand-new or empty project (AGENTS.md, handoff.md, README, src/tests/docs); existing projects should use the built-in /init
---

Load and follow the `project-init` Skill.

Use this only for brand-new or empty directories. For an existing project that already has code and an `AGENTS.md`, use OpenCode's built-in `/init` instead, which infers rules from the actual codebase.
